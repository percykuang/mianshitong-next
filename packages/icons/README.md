# Icons 包说明

`@mianshitong/icons` 负责仓库内复用的图标资源，包括 Remix Icon 别名导出和少量自定义 SVG 图标。

## 开发约定

1. 根入口只导出稳定图标名，不通过 `export *` 暴露内部文件结构。
2. 新增图标时优先放到对应语义分组文件，再决定是否暴露到根入口。
3. 对业务层，`@mianshitong/ui` 仍然是更高层的默认入口；只有在确实需要直接消费图标时，才直接依赖本包。
